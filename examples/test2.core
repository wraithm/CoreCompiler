-- Lambda lift test
true x y = x;
false x y = y;

equals x y = true;

lettest = let x = 3 in twice twice I x;

fact n = if (n == 0) 1 (n * fact (n - 1));

--main = letrec f = \x. if 1 3 (f x) in f 3;
main = fact 1;
